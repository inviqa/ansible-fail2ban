---
- name: Remove local Fail2Ban test containers
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Remove test containers when inventory requests them
      ansible.builtin.include_tasks: tasks/remove_containers.yml
      when: groups.get('fail2ban_test_hosts', []) | length > 0
